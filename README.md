# Python Science Project

This project utilizes pandas, numpy & matplotlib to collect and create visualization of data.

Install Pandas:

        pip install pandas

Install Numpy:

        pip install numpy

Install Matplotlib:

        pip install matplotlib
